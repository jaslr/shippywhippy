# Define the root directory for the project
root = /home/jasonr/shippywippy/shippywippy

# Specify the WSL path prefix
wsl_prefix = \\wsl.localhost\Ubuntu

# Ignore certain directories or files (if needed)
# ignore = node_modules, .git, *.log

# Set the default file creation path
default_create_path = ${wsl_prefix}${root}